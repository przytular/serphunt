version: '3'
services:
  web:
    build: .
    hostname: web
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
  db:
    hostname: db
    image: postgres:11.4
    expose:
      - "5432"
    volumes:
      - .:/code
